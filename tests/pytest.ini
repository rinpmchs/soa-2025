[pytest]
addopts = --strict-markers --tb=short -ra
asyncio_mode = auto
testpaths =
    tests
markers =
    asyncio: mark test as asyncio
    e2e: mark end-to-end tests
    service: mark service-level tests
    kafka: mark Kafka message tests
